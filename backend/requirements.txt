# FastAPI Backend Requirements
# MRI Image Preprocessing System

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Image Processing
opencv-python>=4.10.0
numpy>=1.26.0
Pillow>=10.4.0

# Medical Image Formats (optional - for DICOM and NIfTI support)
# If these fail to install, the API will still work for standard image formats (JPG, PNG)
pydicom>=3.0.0
nibabel>=5.2.0

# Utilities
python-dateutil>=2.9.0
